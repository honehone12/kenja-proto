syntax = "proto3";

package kenja_anime_search;

service AnimeSearch {
    rpc Search(Keyword) returns (stream Candidate);
}

message Keyword {
    string keyword = 1;
}

message Candidate {
    string name = 1;
}
